# 🚀 部署到 GitHub + Vercel

你的原型已经准备好了！按以下步骤部署。

## 📝 第一步：推送到 GitHub

### 方法 A：使用 SSH（推荐）

```bash
# 1. 检查是否已有 SSH key
ls -la ~/.ssh/id_ed25519

# 如果没有，生成新的
ssh-keygen -t ed25519 -C "your-email@example.com"

# 2. 复制公钥
cat ~/.ssh/id_ed25519.pub | pbcopy

# 3. 在浏览器中访问并添加 SSH key:
#    https://github.com/settings/keys
#    点击 "New SSH key"
#    标题：Mac SSH Key
#    粘贴公钥
#    点击 "Add SSH key"

# 4. 设置 Git remote 使用 SSH
cd /Users/yxing/Downloads/pf-dev/patternfly-react-seed
git remote set-url origin git@github.com:patternfly/patternfly-react-seed.git

# 5. 推送代码
git push origin ai_enabled
```

### 方法 B：使用 HTTPS

```bash
# 直接推送，会提示输入用户名和密码
cd /Users/yxing/Downloads/pf-dev/patternfly-react-seed
git push origin ai_enabled
```

如果提示密码错误，使用 Personal Access Token:
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 复制 token，用这个作为密码输入

---

## 🌐 第二步：部署到 Vercel

### 1. 访问 Vercel
打开浏览器访问：https://vercel.com

### 2. 登录
- 点击 "Continue with GitHub"
- 使用你的 GitHub 账号授权

### 3. 导入项目
- 点击 "Add New Project" 按钮
- 找到 "patternfly-react-seed" repository
- 点击 "Import"

### 4. 配置项目
在设置页面，确保：
```
Root Directory: patternfly-react-seed
Framework Preset: Other
Build Command: npm run build
Output Directory: dist
```

**重要：** 在 "Environment Variables" 不需要添加任何变量。

### 5. 选择分支
- 从下拉菜单选择：`ai_enabled`
- 点击 "Deploy"

### 6. 等待部署
- Vercel 会自动构建
- 等待 1-2 分钟
- 看到绿色 "Deployed" 标记

### 7. 访问你的原型
点击 "Visit" 按钮或复制 URL
格式类似：`https://patternfly-react-seed-xxxxx.vercel.app`

---

## ✅ 完成！

你的原型现在在线了！

### 📤 分享链接

把 URL 分享给团队：
```
大家好！

我创建了 UXD 原型，可以在线访问：
https://your-url.vercel.app

功能：
✓ Developer Portal
✓ API 详情页
✓ 过滤和收藏功能
✓ 基于角色的访问控制

欢迎反馈！
```

---

## 🔄 更新原型

当你修改代码后：

```bash
# 1. 提交更改
cd /Users/yxing/Downloads/pf-dev/patternfly-react-seed
git add .
git commit -m "更新描述"
git push origin ai_enabled

# 2. Vercel 会自动重新部署（约30秒）
```

---

## 🌟 Vercel 的优势

- ✅ **自动部署**：每次 push 自动重新部署
- ✅ **预览链接**：每个分支都有独立 URL
- ✅ **HTTPS**：自动 HTTPS 证书
- ✅ **全球 CDN**：快速访问
- ✅ **免费**：个人项目完全免费

---

## 📚 详细文档

查看更详细的部署说明：
- `GITHUB_VERCEL_DEPLOY.md` - 完整的 GitHub + Vercel 指南
- `DEPLOYMENT.md` - 其他部署选项（Netlify, Surge 等）

---

## 🆘 遇到问题？

### GitHub 推送问题
```bash
# 检查当前 remote
git remote -v

# 查看认证信息
git config --list | grep credential
```

### Vercel 部署失败
1. 查看 Build Logs
2. 确认 build 命令是 `npm run build`
3. 确认输出目录是 `dist`

### 路由不工作
- Vercel 自动处理 React Router
- 如果不行，检查 `index.html` 配置

---

## 🎯 当前状态

✅ 代码已提交  
✅ 需要推送到 GitHub  
✅ 然后在 Vercel 导入项目

**现在可以开始部署了！** 🚀

